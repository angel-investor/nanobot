Role & Task:
你现在的身份是一位资深系统架构师。我的目标是基于当前已运行的 HKUDS/nanobot 项目（已接入 DeepSeek API），构建一个名为 JARVIS 的高度集成化个人助手。它不仅要具备冷静、机智的管家性格，还要能深度处理我的开发任务。

Step-by-Step Instructions:

Step 1: 环境与架构扫描

请首先阅读并分析当前仓库的代码结构。重点查看 nanobot 是如何定义 Agent 类的，以及它的 Tool 注册机制和消息循环（Message Loop）逻辑。

确认 DeepSeek API 的配置位置，并评估如何优化其 Prompt 模板以支持复杂的推理。

Step 2: 核心人格与思维链（CoT）实现

在 agents/ 目录下创建一个 jarvis_agent.py（或修改现有的核心智能体）。

人格设计：为其编写 System Prompt。要求：语气礼貌、幽默（类似电影），必须体现出“预判需求”的主动性。

思维强化：利用 DeepSeek 的推理能力，在回复前强制进行 <thought> 内部思考。要求它在执行任何代码或回复前，先分析任务的安全性、必要性和最优路径。

Step 3: 打造多智能体“指挥中心”

修改任务分发逻辑，将 JARVIS 设为主控（Master）。

自动创建并关联两个子 Agent：

Coder_Agent：专注于代码审计和生成（针对我正在研究的 MLLM 和 Crab 项目背景）。

Sys_Monitor：负责监控系统状态、文件变动及执行终端命令。

实现主控对子 Agent 的调度逻辑：JARVIS 接收指令 -> 拆解任务 -> 指派子 Agent -> 汇总结果。

Step 4: 扩展“贾维斯之手”（工具链集成）

请为项目新增一个工具包 tools/os_tools.py，实现以下功能：

全域搜索：基于关键字快速检索我本地的项目文档或代码。

视觉感知钩子：编写一个调用系统截图并保存的函数，预留出将图片传给多模态模型（如 GPT-4o 或 Qwen-VL）的接口。

剪贴板管理：允许 JARVIS 读取和写入系统剪贴板。

Step 5: 长期记忆与偏好同步

在 memory/ 模块中实现一个轻量级的持久化层（如 JSON 或 SQLite）。

让 JARVIS 记录我的特定偏好（例如：我对车载系统、Longjing 茶、以及 iPad 刷机技术的关注），并在对话中自然地体现出它“记得这些”。

Step 6: 入口与交互优化

创建一个 jarvis_main.py 作为全新的启动入口。

优化终端输出样式（使用 rich 库或其他工具），让多智能体之间的协作日志以美观、清晰的方式呈现，模拟贾维斯的 UI 质感。

Constraint:

所有的修改必须保持异步（asyncio）兼容。

在修改或删除原有核心代码前，请先向我确认。

每一阶段完成后，请尝试运行一个简单的测试用例（例如：“JARVIS，帮我看看当前目录结构，并预测我下一步可能要做的修改”）来验证功能。